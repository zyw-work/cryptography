# Lab1：穷举法破译凯撒密码 实验报告

## 1. 正确的密钥 k
**k = 20**

## 2. 解密后的明文
**TALKISCHEAPSHOWMETHECODE**

(注：这是一句著名的程序员格言，断句后为 "Talk is cheap. Show me the code.")

## 3. 判断依据
我通过运行程序输出了所有 25 种可能的结果。在这些结果中，只有当 `k=20` 时，输出的内容是由有意义的英文单词组成的句子。其他密钥对应的输出均为无意义的乱码。因此可以确定 `k=20` 是正确的密钥。