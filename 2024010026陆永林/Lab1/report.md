# Lab1 穷举法破译凯撒密码 - 实验报告

## 1. 实验结果
- 正确密钥 k：12
- 解密后的明文：IMPORTANTINFORMATIONISHERE
## 2. 判断依据
1. 运行`caesar.py`后，枚举了1~25所有密钥的解密结果；
2. 其中仅 k=12 对应的解密结果是连续、有意义的英文单词/语句（如上述明文）；
3. 其余24个密钥的解密结果均为无意义的字母组合（如 k=1 对应 MTEDBLAVXTILAHOEXMAXVHWX），因此判断 k=12 是正确密钥。

## 3. 补充说明
凯撒密码的核心是「字母移位」，由于密钥范围仅1~25，穷举法是最直接的破解方式；判断正确明文的核心是「结果是否有语义」（英文语境下）。